# 篇四 参考手册篇（Reference）

本篇是速查手册，不是教程。它把 Caffe 的层目录（Layer Catalogue）与 solver.prototxt 的配置参数集中在一处，供写 prototxt 时随手查阅。第 1 章说明模型定义方式以及层、prototxt、`caffe.proto` 三者的关系；第 2 章到第 9 章按数据类型、视觉、循环、常用、归一化、激活、工具、损失等类别列出各类层，成段说明保留英文原文并逐段给出中文翻译；第 10 章逐个说明 solver.prototxt 的参数含义与取值；第 11 章到第 13 章分别是脚本模板、配置模板与编译开关速查。阅读本篇之前，最好已经知道一个 Caffe 模型是由 prototxt 描述的，并且跑通过一次训练。

## 本篇目录

- [1 层目录总览（Layer Catalogue Overview）](#1-层目录总览layer-catalogue-overview)
- [2 数据层（Data Layers）](#2-数据层data-layers)
- [3 视觉层（Vision Layers）](#3-视觉层vision-layers)
- [4 循环层（Recurrent Layers）](#4-循环层recurrent-layers)
- [5 常用层（Common Layers）](#5-常用层common-layers)
- [6 归一化层（Normalization Layers）](#6-归一化层normalization-layers)
- [7 激活层与神经元层（Activation / Neuron Layers）](#7-激活层与神经元层activation--neuron-layers)
- [8 工具层（Utility Layers）](#8-工具层utility-layers)
- [9 损失层（Loss Layers）](#9-损失层loss-layers)
- [10 solver.prototxt 参数速查（Solver Configuration Reference）](#10-solverprototxt-参数速查solver-configuration-reference)
- [11 脚本模板（Script Templates）](#11-脚本模板script-templates)
- [12 配置模板（Config Templates）](#12-配置模板config-templates)
- [13 编译开关速查（Build Flags Reference）](#13-编译开关速查build-flags-reference)

---

## 1 层目录总览（Layer Catalogue Overview）

本章说明 Caffe 模型是怎么定义出来的，以及层、prototxt、`caffe.proto` 三者之间的关系。

### 1.1 模型定义与层（Model Definition）

To create a Caffe model you need to define the model architecture in a protocol buffer definition file (prototxt).

> 译：要创建一个 Caffe 模型，你需要在 protocol buffer 定义文件（prototxt）中定义模型结构。

Caffe layers and their parameters are defined in the protocol buffer definitions for the project in `caffe.proto`.

> 译：Caffe 的各个层及其参数，定义在本项目的 protocol buffer 定义文件 `caffe.proto` 中。

### 1.2 本篇的组织方式（How This Reference Is Organized）

接下来第 2 章到第 9 章，按层目录本身的分类依次列出各类层。层清单以表格给出，层名与说明文字保留英文原文，不逐行翻译；某一类层在原文中有成段描述时，先给出英文原文，紧跟以「译：」开头的中文翻译。第 10 章逐个说明 solver.prototxt 的参数。第 11 章到第 13 章是脚本、配置与编译开关速查：脚本与配置片段按原文逐字保留，同一份模板在不同示例中的取值差异集中列在参数表里，编译开关汇总成一张表。

---

## 2 数据层（Data Layers）

数据层是数据进入 Caffe 的入口，位于网络底部。

Data enters Caffe through data layers: they lie at the bottom of nets.

> 译：数据通过数据层进入 Caffe：它们位于网络的底部。

Data can come from efficient databases (LevelDB or LMDB), directly from memory, or, when efficiency is not critical, from files on disk in HDF5 or common image formats.

> 译：数据可以来自高效的数据库（LevelDB 或 LMDB）、直接来自内存，或者在效率不是关键时来自磁盘上的 HDF5 文件或常见图像格式文件。

Common input preprocessing (mean subtraction, scaling, random cropping, and mirroring) is available by specifying `TransformationParameter`s by some of the layers.

> 译：常见的输入预处理（减均值、缩放、随机裁剪和镜像）可以通过在部分层中指定 `TransformationParameter` 来启用。

The bias, scale, and crop layers can be helpful with transforming the inputs, when `TransformationParameter` isn’t available.

> 译：当 `TransformationParameter` 不可用时，bias、scale 和 crop 层有助于对输入做变换。

下面列出数据层，层名与说明保留英文原文。

| 层 | 说明 |
| --- | --- |
| Image Data | read raw images. |
| Database | read data from LEVELDB or LMDB. |
| HDF5 Input | read HDF5 data, allows data of arbitrary dimensions. |
| HDF5 Output | write data as HDF5. |
| Input | typically used for networks that are being deployed. |
| Window Data | read window data file. |
| Memory Data | read data directly from memory. |
| Dummy Data | for static data and debugging. |

Note that the Python Layer can be useful for create custom data layers.

> 译：注意，Python Layer 对于创建自定义数据层会很有用。

---

## 3 视觉层（Vision Layers）

视觉层通常以图像为输入、以图像为输出。

Vision layers usually take images as input and produce other images as output, although they can take data of other types and dimensions.

> 译：视觉层通常以图像作为输入，并产生其他图像作为输出，尽管它们也可以接受其他类型和维度的数据。

A typical “image” in the real-world may have one color channel (`c=1`), as in a grayscale image, or three color channels (`c=3`) as in an RGB (red, green, blue) image.

> 译：现实世界中典型的“图像”可能有 1 个颜色通道（`c=1`），例如灰度图像；也可能有 3 个颜色通道（`c=3`），例如 RGB（红、绿、蓝）图像。

But in this context, the distinguishing characteristic of an image is its spatial structure: usually an image has some non-trivial height `h>1` and width `w>1`.

> 译：但在这里的语境下，图像之所以为图像，在于它的空间结构：通常图像具有不可忽略的高度 `h>1` 和宽度 `w>1`。

This 2D geometry naturally lends itself to certain decisions about how to process the input.

> 译：这种二维几何特性天然地决定了处理输入时可以采用的一些做法。

In particular, most of the vision layers work by applying a particular operation to some region of the input to produce a corresponding region of the output.

> 译：具体来说，大多数视觉层的工作方式是对输入的某个区域施加特定的运算，从而产生输出中与之对应的区域。

In contrast, other layers (with few exceptions) ignore the spatial structure of the input, effectively treating it as “one big vector” with dimension `chw`.

> 译：相比之下，其他层（极少数例外）会忽略输入的空间结构，实际上把它当作维度为 `chw` 的“一个大向量”来处理。

下面列出视觉层，层名与说明保留英文原文。

| 层 | 说明 |
| --- | --- |
| Convolution Layer | convolves the input image with a set of learnable filters, each producing one feature map in the output image. |
| Pooling Layer | max, average, or stochastic pooling. |
| Spatial Pyramid Pooling (SPP) | — |
| Crop | perform cropping transformation. |
| Deconvolution Layer | transposed convolution. |
| Im2Col | relic helper layer that is not used much anymore. |

---

## 4 循环层（Recurrent Layers）

下面列出循环层，层名与说明保留英文原文。

| 层 | 说明 |
| --- | --- |
| Recurrent | — |
| RNN | — |
| Long-Short Term Memory (LSTM) | — |

---

## 5 常用层（Common Layers）

下面列出常用层，层名与说明保留英文原文。

| 层 | 说明 |
| --- | --- |
| Inner Product | fully connected layer. |
| Dropout | — |
| Embed | for learning embeddings of one-hot encoded vector (takes index as input). |

---

## 6 归一化层（Normalization Layers）

下面列出归一化层，层名与说明保留英文原文。

| 层 | 说明 |
| --- | --- |
| Local Response Normalization (LRN) | performs a kind of “lateral inhibition” by normalizing over local input regions. |
| Mean Variance Normalization (MVN) | performs contrast normalization / instance normalization. |
| Batch Normalization | performs normalization over mini-batches. |

The bias and scale layers can be helpful in combination with normalization.

> 译：bias 和 scale 层与归一化层配合使用时会有帮助。

---

## 7 激活层与神经元层（Activation / Neuron Layers）

激活／神经元层是逐元素算子。

In general, activation / Neuron layers are element-wise operators, taking one bottom blob and producing one top blob of the same size.

> 译：一般来说，激活／神经元层是逐元素（element-wise）算子，接收一个 bottom blob 并产生一个尺寸相同的 top blob。

In the layers below, we will ignore the input and out sizes as they are identical:

> 译：在下面列出的层中，我们不再说明输入和输出尺寸，因为二者完全相同：

| 端口 | 尺寸 |
| --- | --- |
| Input | n * c * h * w |
| Output | n * c * h * w |

下面列出激活／神经元层，层名与说明保留英文原文。

| 层 | 说明 |
| --- | --- |
| ReLU / Rectified-Linear and Leaky-ReLU | ReLU and Leaky-ReLU rectification. |
| PReLU | parametric ReLU. |
| ELU | exponential linear rectification. |
| Sigmoid | — |
| TanH | — |
| Absolute Value | — |
| Power | f(x) = (shift + scale * x) ^ power. |
| Exp | f(x) = base ^ (shift + scale * x). |
| Log | f(x) = log(x). |
| BNLL | f(x) = log(1 + exp(x)). |
| Threshold | performs step function at user defined threshold. |
| Bias | adds a bias to a blob that can either be learned or fixed. |
| Scale | scales a blob by an amount that can either be learned or fixed. |

---

## 8 工具层（Utility Layers）

下面列出工具层，层名与说明保留英文原文。

| 层 | 说明 |
| --- | --- |
| Flatten | — |
| Reshape | — |
| Batch Reindex | — |
| Split | — |
| Concat | — |
| Slicing | — |
| Eltwise | element-wise operations such as product or sum between two blobs. |
| Filter / Mask | mask or select output using last blob. |
| Parameter | enable parameters to be shared between layers. |
| Reduction | reduce input blob to scalar blob using operations such as sum or mean. |
| Silence | prevent top-level blobs from being printed during training. |
| ArgMax | — |
| Softmax | — |
| Python | allows custom Python layers. |

---

## 9 损失层（Loss Layers）

损失层通过比较输出与目标来驱动学习。

Loss drives learning by comparing an output to a target and assigning cost to minimize.

> 译：损失通过把输出与目标进行比较、并给出一个待最小化的代价来驱动学习。

The loss itself is computed by the forward pass and the gradient w.r.t. to the loss is computed by the backward pass.

> 译：损失本身由前向传播计算，而关于损失的梯度由反向传播计算。

下面列出损失层，层名与说明保留英文原文。

| 层 | 说明 |
| --- | --- |
| Multinomial Logistic Loss | — |
| Infogain Loss | a generalization of MultinomialLogisticLossLayer. |
| Softmax with Loss | computes the multinomial logistic loss of the softmax of its inputs. It’s conceptually identical to a softmax layer followed by a multinomial logistic loss layer, but provides a more numerically stable gradient. |
| Sum-of-Squares / Euclidean | computes the sum of squares of differences of its two inputs, |
| Hinge / Margin | The hinge loss layer computes a one-vs-all hinge (L1) or squared hinge loss (L2). |
| Sigmoid Cross-Entropy Loss | computes the cross-entropy (logistic) loss, often used for predicting targets interpreted as probabilities. |
| Accuracy / Top-k layer | scores the output as an accuracy with respect to target – it is not actually a loss and has no backward step. |
| Contrastive Loss | — |

Sum-of-Squares / Euclidean 的公式，按 PDF 提取原文保留（原式为多行公式，提取后已串行并缺失符号）：

```text
inputs, 12N∑Ni=1∥x1i−x2i∥22
∑=
‖ 𝑥
 −𝑥
‖
.
```

<!-- 原文此处疑似缺字 -->

---

## 10 solver.prototxt 参数速查（Solver Configuration Reference）

本章逐个说明 solver.prototxt 中各参数的含义与取值。

The solver.prototxt is a configuration file used to tell caffe how you want the network trained.

> 译：solver.prototxt 是一个配置文件，用来告诉 caffe 你希望如何训练网络。

原文为逐条参数说明文档。需要注意的是：提取出的文本里只有参数名与说明文字，并没有给出可直接使用的完整 prototxt 实例，因此下文只还原原文写出的参数名、说明与可选项，不补写任何原文之外的取值或示例配置。

原文中每个参数名前面带一个 `>` 标记（如 `> base_lr`），下面把这一标记转成小节标题，参数名本身与说明文字保持原样。

原文中的参数名按出现顺序列在下面。此处仅是参数名本身，不是一份可运行的配置；原文对 `snapshot_prefix` 与 `net` 的写法带冒号，这里保持原样。

```prototxt
base_lr
lr_policy
gamma
stepsize
stepvalue
max_iter
momentum
weight_decay
solver_mode
snapshot
snapshot_prefix:
net:
test_iter
test_interval
display
type
```

除上面这份清单之外，其他笔记里还出现过 `power`、`momentum2`、`delta`（其中一处拼作 `momemtum`）、`rms_decay`、`regularization_type`、`decay_mult`，以及 `lr_policy` 的取值 `"inv"`；这些内容一并列在本章 10.17 到 10.22 节，每条下方注明它出现在哪份笔记。

### 10.1 base_lr

This parameter indicates the base (beginning) learning rate of the network. The value is a real number (floating point).

> 译：该参数表示网络的基础（起始）学习率。取值为实数（浮点数）。

### 10.2 lr_policy

This parameter indicates how the learning rate should change over time. This value is a quoted string.

> 译：该参数表示学习率应如何随时间变化。取值为带引号的字符串。

Options include:

> 译：可选项包括：

- "step" - drop the learning rate in step sizes indicated by the gamma parameter.
- "multistep" - drop the learning rate in step size indicated by the gamma at each specified stepvalue.
- "fixed" - the learning rate does not change.
- "exp" - gamma^iteration
- "poly" - <!-- 原文此处疑似缺字 -->
- "sigmoid" - <!-- 原文此处疑似缺字 -->
- "inv"

另有 `"inv"` 这个取值：它出现在求解器方法的说明一节（那里的训练日志里是 `lr_policy: "inv"` 配 `power: 0.75`），未见于参数清单，原文没有给出它的说明文字。

### 10.3 gamma

This parameter indicates how much the learning rate should change every time we reach the next "step." The value is a real number, and can be thought of as multiplying the current learning rate by said number to gain a new learning rate.

> 译：该参数表示每次进入下一个 “step” 时学习率应变化多少。取值为实数，可以理解为用当前学习率乘以这个数来得到新的学习率。

### 10.4 stepsize

This parameter indicates how often (at some iteration count) that we should move onto the next "step" of training. This value is a positive integer.

> 译：该参数表示每隔多少次迭代（以某个迭代计数为准）就进入训练的下一步 “step”。取值为正整数。

### 10.5 stepvalue

This parameter indicates one of potentially many iteration counts that we should move onto the next "step" of training. This value is a positive integer. There are often more than one of these parameters present, each one indicated the next step iteration.

> 译：该参数表示进入训练下一个 “step” 的迭代计数之一，这样的计数可能有很多个。取值为正整数。这个参数常常会出现不止一个，每一个都指明下一个 step 的迭代次数。

### 10.6 max_iter

This parameter indicates when the network should stop training. The value is an integer indicate which iteration should be the last.

> 译：该参数表示网络应在何时停止训练。取值为整数，指明哪一次迭代应当是最后一次。

### 10.7 momentum

This parameter indicates how much of the previous weight will be retained in the new calculation. This value is a real fraction.

> 译：该参数表示此前的权重有多少会保留到新的计算中。取值为一个实数比例。

### 10.8 weight_decay

This parameter indicates the factor of (regularization) penalization of large weights. This value is a often a real fraction.

> 译：该参数表示对大权重施加（正则化）惩罚的系数。取值通常是一个实数比例。

### 10.9 solver_mode

This parameter indicates which mode will be used in solving the network.

> 译：该参数表示求解网络时使用哪一种模式。

Options include:

> 译：可选项包括：

- CPU

- GPU

### 10.10 snapshot

This parameter indicates how often caffe should output a model and solverstate. This value is a positive integer.

> 译：该参数表示 caffe 每隔多久输出一次模型与 solverstate。取值为正整数。

### 10.11 snapshot_prefix

This parameter indicates how a snapshot output's model and solverstate's name should be prefixed. This value is a double quoted string.

> 译：该参数表示快照输出的模型与 solverstate 文件名应加什么前缀。取值为双引号字符串。

（原文中该参数名紧接在 `snapshot` 的说明之后单独成行，未带其他参数名前面的 `>` 标记。）

### 10.12 net

This parameter indicates the location of the network to be trained (path to prototxt). This value is a double quoted string.

> 译：该参数表示待训练网络所在的位置（prototxt 的路径）。取值为双引号字符串。

### 10.13 test_iter

This parameter indicates how many test iterations should occur per test_interval. This value is a positive integer.

> 译：该参数表示每个 test_interval 内应执行多少次测试迭代。取值为正整数。

### 10.14 test_interval

This parameter indicates how often the test phase of the network will be executed.

> 译：该参数表示网络的测试阶段多久执行一次。

### 10.15 display

This parameter indicates how often caffe should output results to the screen. This value is a positive integer and specifies an iteration count.

> 译：该参数表示 caffe 每隔多久向屏幕输出一次结果。取值为正整数，指定的是迭代次数。

### 10.16 type

This parameter indicates the back propagation algorithm used to train the network. This value is a quoted string.

> 译：该参数表示训练网络所用的反向传播算法。取值为带引号的字符串。

Options include:

> 译：可选项包括：

- Stochastic Gradient Descent "SGD"
- AdaDelta "AdaDelta"
- Adaptive Gradient "AdaGrad"
- Adam "Adam"
- Nesterov’s Accelerated Gradient "Nesterov"
- RMSprop "RMSProp"

### 10.17 power

该参数出现在求解器方法的说明一节，未见于参数清单。那里的训练日志中它是与 `lr_policy: "inv"` 一起出现的：

```text
lr_policy: "inv"
gamma: 0.0001
power: 0.75
```

> 原文没有给出该参数的说明文字，这里只搬运原文出现的取值。

### 10.18 momentum2

该参数出现在求解器方法的说明（Adam 一节）中，未见于参数清单。

Kingma et al. [1] proposed to use `β1=0.9,β2=0.999,ε=10−8` as default values. Caffe uses the values of `momemtum`, `momentum2`, `delta` for `β1,β2,ε`, respectively.

> 译：Kingma 等人 [1] 提出使用 `β1=0.9,β2=0.999,ε=10−8` 作为默认值。Caffe 分别用 `momemtum`、`momentum2`、`delta` 的值作为 `β1,β2,ε`。

原文此处把 momentum 拼作 `momemtum`，按原文保留。

### 10.19 delta

该参数出现在上面同一句（求解器方法的说明，Adam 一节）中，未见于参数清单。

Kingma et al. [1] proposed to use `β1=0.9,β2=0.999,ε=10−8` as default values. Caffe uses the values of `momemtum`, `momentum2`, `delta` for `β1,β2,ε`, respectively.

> 译：Kingma 等人 [1] 提出使用 `β1=0.9,β2=0.999,ε=10−8` 作为默认值。Caffe 分别用 `momemtum`、`momentum2`、`delta` 的值作为 `β1,β2,ε`。

### 10.20 rms_decay

该参数出现在求解器方法的说明（RMSprop 一节）中，未见于参数清单。

The default value of δ ( `rms_decay` ) is set to `δ=0.99`.

> 译：δ（`rms_decay`）的默认值被设为 `δ=0.99`。

### 10.21 regularization_type

该参数出现在权重衰减的说明中，未见于参数清单。

In the solver file, we can set a global regularization loss using the weight_decay and regularization_type options.

> 译：在 solver 文件中，我们可以用 weight_decay 与 regularization_type 这两个选项来设置全局正则化损失。

Caffe also allows you to choose between L2 regularization (default, sum of weight squares) and L1 regularization (sum of weight absolute values), by setting

> 译：Caffe 也允许你在 L2 正则化（默认，权重平方和）与 L1 正则化（权重绝对值之和）之间选择，方法是设置

```prototxt
regularization_type: "L1"
```

### 10.22 decay_mult

该参数出现在权重衰减的说明中，未见于参数清单。

In many cases we want different weight decay rates for different layers. This can be done by setting the decay_mult option for each layer in the network definition file, where decay_mult is the multiplier on the global weight decay rate, so the actual weight decay rate applied for one layer is decay_mult*weight_decay.

> 译：很多情况下我们希望对不同的层使用不同的权重衰减率。这可以通过在网络定义文件中为每一层设置 decay_mult 选项来实现，其中 decay_mult 是全局权重衰减率上的乘数，因此某一层实际生效的权重衰减率是 decay_mult*weight_decay。

For example, the following defines a convolutional layer with NO weight decay regardless of the options in the solver file.

> 译：例如，下面定义了一个卷积层，无论 solver 文件中如何设置，它都不做权重衰减。

```prototxt
layer {
  name: "Convolution1"
  type: "Convolution"
  bottom: "data"
  top: "Convolution1"
  param {
    decay_mult: 0
  }
  convolution_param {
    num_output: 32
    pad: 0
    kernel_size: 3
    stride: 1
    weight_filler {
      type: "xavier"
    }
  }
}
```

### 10.23 参数与取值一览（Parameter Value Reference）

下表汇总各参数的取值与类型。表中取值/类型一栏直接取自原文的表述，未做补充。

| 参数 | 原文所述取值或类型 |
| --- | --- |
| `base_lr` | a real number (floating point) |
| `lr_policy` | a quoted string；原文列出可选值 "step"、"multistep"、"fixed"、"exp"、"poly"、"sigmoid" |
| `lr_policy`（补充取值） | 求解器方法的说明一节里另有 "inv"（训练日志为 `lr_policy: "inv"` 配 `power: 0.75`），未见于参数清单 |
| `gamma` | a real number |
| `stepsize` | a positive integer |
| `stepvalue` | a positive integer；原文说明通常会有多个该参数 |
| `max_iter` | an integer（原文：indicate which iteration should be the last） |
| `momentum` | a real fraction |
| `weight_decay` | 原文：is a often a real fraction |
| `solver_mode` | 原文列出 CPU、GPU |
| `snapshot` | a positive integer |
| `snapshot_prefix:` | a double quoted string |
| `net:` | a double quoted string（原文：path to prototxt） |
| `test_iter` | a positive integer |
| `test_interval` | 原文未给出取值或类型说明 |
| `display` | a positive integer and specifies an iteration count |
| `type` | a quoted string；原文列出 "SGD"、"AdaDelta"、"AdaGrad"、"Adam"、"Nesterov"、"RMSProp" |
| `power` | 原文只在训练日志中出现取值 `power: 0.75`（与 `lr_policy: "inv"` 同时出现） |
| `momentum2` | 原文：Caffe uses the values of `momemtum`, `momentum2`, `delta` for `β1,β2,ε`（对应 β2） |
| `delta` | 同上句（对应 ε） |
| `rms_decay` | 原文：The default value of δ ( `rms_decay` ) is set to `δ=0.99` |
| `regularization_type` | 原文列出 L2（default）与 L1；示例写法 `regularization_type: "L1"` |
| `decay_mult` | 原文：the multiplier on the global weight decay rate；示例取值 `decay_mult: 0` |

---

## 11 脚本模板（Script Templates）

本章收录两份可直接改用的脚本：pycaffe 图像分类预测脚本，以及 DIGITS 用的 MNIST 数据集转换脚本。

### 11.1 pycaffe 图像分类预测脚本（Image Classification Prediction Script）

两份预测脚本只在下表列出的四行上不同，其余内容逐行一致。下面以 GoogLeNet 版本为模板给出全文，要改成 ROP 版本时按表中内容替换这四行即可。

| 脚本中不同的行 | GoogLeNet 版本 | ROP 版本 |
| --- | --- | --- |
| `MODEL_FILE` | `MODEL_FILE =caffe_root+'models/bvlc_googlenet/deploy.prototxt'` | `MODEL_FILE =caffe_root+'models/finetune_alexnet_rop/deploy.prototxt'` |
| `PRETRAINED` | `PRETRAINED =caffe_root+'models/bvlc_googlenet/bvlc_googlenet.caffemodel'` | `PRETRAINED =caffe_root+'models/finetune_alexnet_rop/finetune_alexnet_rop_iter_100000.caffemodel'` |
| `imagenet_labels_filename` | `imagenet_labels_filename = caffe_root +'data/ilsvrc12/synset_words.txt'` | `imagenet_labels_filename = caffe_root +'models/finetune_alexnet_rop/rop/labels.txt'` |
| `os.walk` 一行 | `for root,dirs,files in os.walk(caffe_root + "examples/images/"):` | `for root,dirs,files in os.walk(caffe_root + "examples/images/ROP/"):` |

模板全文（原文为 `test_googlenet.py`）：

```python
#coding=utf-8

import numpy
import os
import sys
import caffe


caffe_root = '/home/zys/caffe-master/'
sys.path.insert(0,caffe_root+'Python')


MODEL_FILE =caffe_root+'models/bvlc_googlenet/deploy.prototxt'
PRETRAINED =caffe_root+'models/bvlc_googlenet/bvlc_googlenet.caffemodel'

#cpu模式

caffe.set_mode_gpu()

#定义使用的神经网络模型
#image_dims : dimensions to scale input for cropping/sampling.
#        Default is to scale to net input size for whole-image crop.
#    mean, input_scale, raw_scale, channel_swap: params for
#        preprocessing options.

net = caffe.Classifier(MODEL_FILE, PRETRAINED, 
               mean=numpy.load(caffe_root +'python/caffe/imagenet/ilsvrc_2012_mean.npy').mean(1).mean(1),
               channel_swap=(2,1,0),
               raw_scale=255,
               # image_dims=(224, 224)
)

imagenet_labels_filename = caffe_root +'data/ilsvrc12/synset_words.txt'
labels =numpy.loadtxt(imagenet_labels_filename, str, delimiter='\t')

#对目标路径中的图像，遍历并分类

for root,dirs,files in os.walk(caffe_root + "examples/images/"):
   for file in files:
       #加载要分类的图片

       IMAGE_FILE = os.path.join(root,file).decode('gbk').encode('utf-8');
       input_image = caffe.io.load_image(IMAGE_FILE)   
       print("\n---- {} ----".format(IMAGE_FILE))
 
       #预测图片类别

       prediction = net.predict([input_image])
       print 'predicted class:',prediction[0].argmax()
 
       # 输出概率最大的前5个预测结果

       top_k = net.blobs['prob'].data[0].flatten().argsort()[-1:-6:-1]
       print labels[top_k]
```

运行命令：

```bash
python test_googlenet.py
```

ROP 版本先设置 `PYTHONPATH` 与 `LD_LIBRARY_PATH`（取值见第 13 章），再执行 `python test_rop.py`。

### 11.2 数据集转换脚本（Dataset Conversion Script）

下面的脚本用于下载并准备 MNIST 数据集；其 `getData()` 的 docstring 写明它 “Downloads the dataset and prepares it for DIGITS consumption”。全文保留如下。

```python
# Download and extract the MNIST dataset
# Modified by zys for Pyhont 3

import gzip
import os
import struct
import urllib.request
import shutil

import numpy as np
import PIL.Image

class MnistDownloader:
    """
    See details about the MNIST dataset here:
    http://yann.lecun.com/exdb/mnist/
    """
    def __init__(self, outdir, clean=False, file_extension='png'):
        """
        Arguments:
        outdir -- directory where to download and create the dataset
        if this directory doesn't exist, it will be created
        Keyword arguments:
        clean -- delete outdir first if it exists
        file_extension -- image format for output images
        """
        self.outdir = outdir
        self.mkdir(self.outdir, clean=clean)
        self.file_extension = file_extension.lower()


    def getData(self):
        """
        This is the main function that should be called by the users!
        Downloads the dataset and prepares it for DIGITS consumption
        """
        for url in self.urlList():
            self.__downloadFile(url)

        self.uncompressData()

        self.processData()
        print ("Dataset directory is created successfully at {}".format(self.outdir))

    def __downloadFile(self, url):
        """
        Downloads the url
        """
        download_path = os.path.join(self.outdir, os.path.basename(url))
        if not os.path.exists(download_path):
            print ("Downloading url={} ...".format(url))
            urllib.request.urlretrieve(url, download_path)

    def mkdir(self, d, clean=False):
        """
        Safely create a directory
        Arguments:
        d -- the directory name
        Keyword arguments:
        clean -- if True and the directory already exists, it will be deleted and recreated
        """
        if os.path.exists(d):
            if clean:
                shutil.rmtree(d)
            else:
                return
        os.mkdir(d)

    def urlList(self):
        return [
            'http://yann.lecun.com/exdb/mnist/train-images-idx3-ubyte.gz',
            'http://yann.lecun.com/exdb/mnist/train-labels-idx1-ubyte.gz',
            'http://yann.lecun.com/exdb/mnist/t10k-images-idx3-ubyte.gz',
            'http://yann.lecun.com/exdb/mnist/t10k-labels-idx1-ubyte.gz',
        ]

    def uncompressData(self):
        for zipped, unzipped in [
                ('train-images-idx3-ubyte.gz',  'train-images.bin'),
                ('train-labels-idx1-ubyte.gz',  'train-labels.bin'),
                ('t10k-images-idx3-ubyte.gz',   'test-images.bin'),
                ('t10k-labels-idx1-ubyte.gz',   'test-labels.bin'),
        ]:
            zipped_path = os.path.join(self.outdir, zipped)
            assert os.path.exists(zipped_path), 'Expected "%s" to exist' % zipped
            unzipped_path = os.path.join(self.outdir, unzipped)
            if not os.path.exists(unzipped_path):
                print ("Uncompressing file={} ...".format(zipped))
                with gzip.open(zipped_path) as infile, open(unzipped_path, 'wb') as outfile:
                    outfile.write(infile.read())

    def processData(self):
        self.__extract_images('train-images.bin', 'train-labels.bin', 'train')
        self.__extract_images('test-images.bin', 'test-labels.bin', 'test')

    def __extract_images(self, images_file, labels_file, phase):
        """
        Extract information from binary files and store them as images
        """
        labels = self.__readLabels(os.path.join(self.outdir, labels_file))
        images = self.__readImages(os.path.join(self.outdir, images_file))
        assert len(labels) == len(images), '%d != %d' % (len(labels), len(images))

        output_dir = os.path.join(self.outdir, phase)
        self.mkdir(output_dir, clean=True)
        with open(os.path.join(output_dir, 'labels.txt'), 'w') as outfile:
            for label in range(10):
                outfile.write('%s\n' % label)
        with open(os.path.join(output_dir, '%s.txt' % phase), 'w') as outfile:
            for index, image in enumerate(images):
                dirname = os.path.join(output_dir, labels[index])
                self.mkdir(dirname)
                filename = os.path.join(dirname, '%05d.%s' % (index, self.file_extension))
                image.save(filename)
                outfile.write('%s %s\n' % (filename, labels[index]))

    def __readLabels(self, filename):
        """
        Returns a list of ints
        """
        print ('Reading labels from {} ...'.format(filename))
        labels = []
        with open(filename, 'rb') as infile:
            infile.read(4)  # ignore magic number
            count = struct.unpack('>i', infile.read(4))[0]
            data = infile.read(count)
            for byte in data:
                # http://stackoverflow.com/questions/42347498/translating-python-2-byte-checksum-calculator-to-python-3                
                label = byte #struct.unpack('>B', byte)[0]
                labels.append(str(label))
        return labels

    def __readImages(self, filename):
        """
        Returns a list of PIL.Image objects
        """
        print ('Reading images from {} ...'.format(filename))
        images = []
        with open(filename, 'rb') as infile:
            infile.read(4)  # ignore magic number
            count = struct.unpack('>i', infile.read(4))[0]
            rows = struct.unpack('>i', infile.read(4))[0]
            columns = struct.unpack('>i', infile.read(4))[0]

            for i in range(count):
                data = infile.read(rows * columns)
                image = np.fromstring(data, dtype=np.uint8)
                image = image.reshape((rows, columns))
                image = 255 - image  # now black digit on white background
                images.append(PIL.Image.fromarray(image))
        return images
    
    
# Execute
MnistDownloader("g:/mnist", False).getData()
```

脚本末尾按示例调用了 `MnistDownloader("g:/mnist", False).getData()`；第二个参数 `False` 对应 `clean`，即不在开始时清空输出目录。

---

## 12 配置模板（Config Templates）

本章给出微调用的 solver.prototxt 模板，以及 train_val.prototxt / deploy.prototxt 中与分类层相关的片段。

### 12.1 微调用的 solver.prototxt 模板（Fine-tuning Solver Template）

两份微调用的 solver.prototxt 只在下表两行上不同，其余内容逐行一致。下面给出其中一份的全文作为模板，另一份按表中取值替换即可。

| 参数行 | finetune_flickr_style | finetune_alexnet_rop |
| --- | --- | --- |
| `net` | `net: "models/finetune_flickr_style/train_val.prototxt"` | `net: "models/finetune_alexnet_rop/train_val.prototxt"` |
| `snapshot_prefix` | `snapshot_prefix: "models/finetune_flickr_style/finetune_flickr_style"` | `snapshot_prefix: "models/finetune_alexnet_rop/finetune_alexnet_rop"` |

模板全文：

```prototxt
net: "models/finetune_flickr_style/train_val.prototxt"
test_iter: 100
test_interval: 1000
# lr for fine-tuning should be lower than when starting from scratch. Original AlexNet uses 0.01
base_lr: 0.001
lr_policy: "step"
gamma: 0.1
# stepsize should also be lower, as we're closer to being done. Original AlexNet uses 100000
stepsize: 20000
display: 20
max_iter: 100000
momentum: 0.9
weight_decay: 0.0005
snapshot: 10000
snapshot_prefix: "models/finetune_flickr_style/finetune_flickr_style"
# uncomment the following to default to CPU mode solving
solver_mode: GPU
```

flickr_style 示例的训练命令：

```bash
./build/tools/caffe train -solver models/finetune_flickr_style/solver.prototxt -weights models/bvlc_reference_caffenet/bvlc_reference_caffenet.caffemodel -gpu 0
```

ROP 示例的训练命令与之相同，只把 `-solver` 的路径换成 `models/finetune_alexnet_rop/solver.prototxt`。

### 12.2 分类层改造片段（train_val.prototxt）

微调时只改动分类层。下面是两份 train_val.prototxt 中分类层的相应片段。

finetune_flickr_style 的 `fc8_flickr`：

```prototxt
layer {
  # rename layer name
  name: "fc8_flickr"
  type: "InnerProduct"
  bottom: "fc7"
  top: "fc8_flickr"
  # lr_mult is set to higher than for other layers, because this layer is starting from random while the others are already trained
  param {
    # Original AlexNet uses 1
    lr_mult: 10
    decay_mult: 1
  }
  param {
    # Original AlexNet uses 2
    lr_mult: 20
    decay_mult: 0
  }
  inner_product_param {
    # change to your number of classes. Original AlexNet has 1000 classes
    num_output: 20
    weight_filler {
      type: "gaussian"
      std: 0.01
    }
    bias_filler {
      type: "constant"
      value: 0
    }
  }
}
```

finetune_alexnet_rop 的 `fc8_rop`：

```prototxt
layer {
  name: "fc8_rop"
  type: "InnerProduct"
  bottom: "fc7"
  top: "fc8_rop"
  # lr_mult is set to higher than for other layers, because this layer is starting from random while the others are already trained
  param {
    lr_mult: 10
    decay_mult: 1
  }
  param {
    lr_mult: 20
    decay_mult: 0
  }
  inner_product_param {
    num_output: 2
    weight_filler {
      type: "gaussian"
      std: 0.01
    }
    bias_filler {
      type: "constant"
      value: 0
    }
  }
}
```

两个片段的差别在于层的 `name` 与 `top`（`fc8_flickr` 与 `fc8_rop`）、`num_output`（20 与 2），以及 flickr 片段中多出的几行注释（原文在注释里说明这些值相对原版 AlexNet 的改动）。

紧跟 `fc8_rop` 之后还有 `accuracy` 与 `loss` 两层：

```prototxt
layer {
  name: "accuracy"
  type: "Accuracy"
  bottom: "fc8_rop"
  bottom: "label"
  top: "accuracy"
  include {
    phase: TEST
  }
}
layer {
  name: "loss"
  type: "SoftmaxWithLoss"
  bottom: "fc8_rop"
  bottom: "label"
  top: "loss"
}
```

### 12.3 deploy.prototxt 中的对应片段（deploy.prototxt Fragment）

deploy.prototxt 中间的 `fc8_rop` 与上面 train_val.prototxt 中的 `fc8_rop` 逐行一致，因此不再重复；这里只列出前后两端的片段。

网络开头的 `Input` 层：

```prototxt
name: "RopCaffeNet"
layer {
  name: "data"
  type: "Input"
  top: "data"
  input_param { shape: { dim: 10 dim: 3 dim: 227 dim: 227 } }
}
… … 
```

网络结尾的 `prob` 层：

```prototxt
layer {
  name: "prob"
  type: "Softmax"
  bottom: "fc8_rop"
  top: "prob"
}
```

---

## 13 编译开关速查（Build Flags Reference）

本章把编译 Caffe、编译 py-faster-rcnn 自带的 Caffe 源码时对 Makefile.config 的改动，以及 pycaffe 运行时的环境变量，汇总成一张表。

| 开关 | 取值 | 作用 | 出现在哪个项目 |
| --- | --- | --- | --- |
| `USE_CUDNN := 1` | uncomment（取消注释） | cuDNN acceleration switch（启用 cuDNN 编译） | BVLC Caffe |
| `USE_CUDNN := 1` | 注释掉：# USE_CUDNN := 1 | cuDNN v5.1 cannot compile，禁用 cuDNN 或降级到 cuDNN v4 | py-faster-rcnn 自带的 Caffe 源码 |
| `INCLUDE_DIRS`（HDF5 头文件路径） | `INCLUDE_DIRS := $(PYTHON_INCLUDE) /usr/local/include /usr/include/hdf5/serial` | 指定 HDF5 头文件所在目录 | BVLC Caffe |
| `LIBRARY_DIRS`（HDF5 库路径） | `LIBRARY_DIRS := $(PYTHON_LIB) /usr/local/lib /usr/lib /usr/lib/i386-linux-gnu/hdf5/serial` | 指定 HDF5 库所在目录 | BVLC Caffe |
| `LIBRARY_DIRS`（HDF5 库路径，另一写法） | `LIBRARY_DIRS := $(PYTHON_LIB) /usr/local/lib /usr/lib /usr/lib/x86_64-linux-gnu/hdf5/serial/` | 同上，原文用 “or” 给出这两种写法 | BVLC Caffe |
| `WITH_PYTHON_LAYER := 1` | 取消注释 | 确保这一行处于取消注释状态 | py-faster-rcnn 的 Makefile.config |
| `OPENCV_VERSION := 3` | 取消注释 | 已安装 OpenCV 3 时使用 | py-faster-rcnn 的 Makefile.config |
| `PYTHON_INCLUDE` | `PYTHON_INCLUDE := /usr/include/python2.7 \` 与 `/usr/local/lib/python2.7/dist-packages/numpy/core/include` | 找到 Python.h 与 numpy/arrayobject.h（原文注释） | BVLC Caffe |
| `PYTHONPATH` / `LD_LIBRARY_PATH` | `export PYTHONPATH=/home/zys/caffe-master/distribute/python`；`export LD_LIBRARY_PATH=/home/zys/caffe-master/distribute/lib` | 设置 pycaffe 运行时的模块与库搜索路径 | pycaffe |

HDF5 的两行改动之前记录的是链接报错 `cannot find -lhdf5_hl` / `cannot find -lhdf5`，原文并用 `sudo find / -name libhdf5.so` 确认库位于 `/usr/lib/x86_64-linux-gnu/hdf5/serial/libhdf5.so`。原文还给出把库路径持久化的做法：编辑 `/etc/ld.so.conf.d/caffe.conf`，加入 `/home/zys/caffe-master/distribute/lib`，再执行 `sudo ldconfig`。
